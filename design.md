# WordSprout design.md

> 项目代号：WordSprout（词芽）  
> 项目类型：面向 1-6 年级小学生的英语单词记忆 Web/PWA 应用  
> 文档目标：定义项目第一版的产品范围、功能设计、技术方案、数据结构、接口、验收标准与实施边界，作为后续 `step-X.md` 拆分和开发执行的唯一设计依据。

---

## 1. 项目背景

小学生英语单词学习存在三个高频问题：

1. 记忆保持差。孩子容易“今天会、明天忘”，学习过程缺少稳定的复习调度机制。
2. 教材不同步。很多单词 APP 以泛词书为核心，不能准确匹配教材版本、年级、学期和单元。
3. 家长难陪伴。家长通常只能看到“学了没学”，看不到学了什么、掌握得怎样，也难以低成本控制时长和节奏。

市场上已有产品通常只解决其中一部分：

- 成人向产品强在 SRS 算法和词汇深度，但弱在低龄适配和教材同步。
- 小学向产品强在教材场景和趣味性，但弱在语义深度、记忆理论落实或可扩展性。

WordSprout 的目标不是做“大而全英语平台”，而是做一个以“教材同步 + 科学记忆 + 多模态学习 + 反成瘾体验”为核心的单词学习工具。

---

## 2. 项目目标

### 2.1 总目标

构建一个可在手机和平板上运行的英语单词记忆 Web/PWA 应用，让小学生能够围绕教材单元完成“学新词 - 做复习 - 得反馈 - 学完即走”的完整闭环。

### 2.2 第一版目标

第一版只追求以下结果：

1. 支持至少 1 套教材的单元级单词学习。
2. 支持基于 SRS 的复习任务调度。
3. 支持五步学习流：看、听、说、写、用。
4. 支持每日学习上限和完成页。
5. 支持家长查看基础学习记录。

### 2.3 非目标

第一版明确不做：

1. 原生 APP。
2. 教师端完整后台。
3. 班级实时 PK 或强社交。
4. 商业支付系统。
5. 多租户权限体系。
6. 大规模内容生产后台。

---

## 3. 目标用户

### 3.1 主用户：学生

- 年龄段：约 6-12 岁
- 年级：1-6 年级
- 典型设备：家长手机、平板、家庭电脑浏览器
- 主要诉求：
  - 跟教材同步学单词
  - 能听、说、写，不只是看中文
  - 学习过程简单、反馈明确
  - 不被广告、无关内容和复杂操作打断

### 3.2 次用户：家长

- 典型角色：陪伴式监督者、规则设置者
- 主要诉求：
  - 知道孩子今天学了什么
  - 能看到正确率、时长、薄弱词
  - 能控制学习上限和时段
  - 不需要深度参与每一步操作

### 3.3 潜在扩展用户

- 教师
- 培训机构
- 内容维护人员

第一版不为这些角色设计完整使用链路，只做后续兼容预留。

---

## 4. 产品原则

1. 记忆科学性优先。每个核心功能必须能对应到明确的记忆理论。
2. 学完即走。第一版不追求停留时长，追求完成率和保持率。
3. 单元级同步。以教材版本、学期、单元为基本组织粒度，而非仅按年级。
4. 多模态优先。单词学习必须覆盖至少视、听、说、写四类通道，并尽量进入语义使用层。
5. 低认知负担。小学生页面信息密度和操作复杂度必须受控。
6. 家长省心。默认减少安装、注册、复杂配置和广告干扰。

---

## 5. 记忆理论映射

| 理论 | 产品要求 | 设计落点 |
|---|---|---|
| 艾宾浩斯遗忘曲线 | 有动态复习机制 | SRS 复习引擎 |
| 加工深度理论 | 进入语义使用层 | 五步学习流中的“用” |
| 多通道编码理论 | 视、听、说、写多通道学习 | 看/听/说/写/用 |
| 主动回忆理论 | 强制用户主动输出 | 拼写、选择、跟读 |
| 语义网络理论 | 支持词汇关联扩展 | Phase 2 词根/搭配/近反义 |
| 自我决定理论 | 轻激励而非无限奖励 | 花园成长、完成页、无排行榜 |

---

## 6. 产品范围与优先级

### 6.1 Must Have

1. 教材版本、年级、学期、单元选择
2. 单元级词库加载
3. 新词学习五步流
4. SRS 复习调度
5. 学习记录与掌握度更新
6. 每日新词/复习上限
7. 完成页
8. 家长基础查看页

### 6.2 Should Have

1. 花园成长可视化
2. 记忆曲线可视化
3. 发音评分基础反馈
4. 错题或薄弱词归档

### 6.3 Could Have

1. 多教材版本扩展
2. PWA 离线缓存
3. 成就分享卡片
4. 语境视频
5. 词根词缀扩展

### 6.4 Won’t Have Now

1. 原生 APP 发布
2. 实时排行榜
3. 复杂会员体系
4. 教师端管理后台
5. 重内容审核系统

---

## 7. 核心用户场景

### 7.1 学生日常学习场景

1. 学生打开应用。
2. 选择教材版本、年级、学期、单元。
3. 查看今日待完成任务：新词数、复习数。
4. 先处理复习任务，再进入新词学习。
5. 对每个单词走完五步学习流。
6. 达到每日上限后进入完成页并退出。

### 7.2 家长查看场景

1. 家长打开家长页。
2. 查看今日或本周学习记录。
3. 查看学习时长、正确率、完成情况、薄弱词。
4. 调整每日上限、允许学习时段、是否学完锁定。

### 7.3 课程演示场景

1. 进入首页。
2. 选择一个教材单元。
3. 展示一个完整的新词学习过程。
4. 展示复习任务。
5. 展示完成页。
6. 展示家长查看页。

---

## 8. 信息架构与页面结构

### 8.1 页面总览

| 页面 | 路由建议 | 说明 |
|---|---|---|
| 首页 | `/` | 项目入口、登录状态、教材入口 |
| 教材选择页 | `/learn/select` | 选择教材/年级/学期/单元 |
| 今日任务页 | `/learn/today` | 显示今日新词和复习任务概览 |
| 学习页 | `/learn/session/:unitId` | 五步学习流主页面 |
| 复习页 | `/review` | 待复习词任务入口 |
| 完成页 | `/done` | 学完即走的日总结页面 |
| 家长页 | `/parent` | 学习记录与设置 |
| 设置页 | `/settings` | 学习上限、时段等 |

### 8.2 页面优先级

第一版重点完成：

1. 首页
2. 教材选择页
3. 今日任务页
4. 学习页
5. 完成页
6. 家长页

复习页如果与学习页共用组件，可以合并为一个流程型页面。

---

## 9. 功能设计

## 9.1 教材同步系统

### 目标

将单词学习入口与教材单元一一对应，避免孩子在泛词书中迷失。

### 要求

1. 支持教材版本、年级、学期、单元四级选择。
2. 每个单元关联一组有顺序的单词。
3. 支持标记单元状态：未开始、学习中、已完成。

### MVP 范围

- 至少 1 套教材
- 至少 1 个年级和 2-4 个单元演示数据

## 9.2 五步学习流

### 目标

把单词学习从“看中文背拼写”扩展为结构化的多模态学习过程。

### 五步定义

1. 看：图片或场景锚点 + 单词呈现
2. 听：播放发音、音节拆分
3. 说：跟读并给出基础反馈
4. 写：拼写练习
5. 用：语境选择或简单造句

### 关键要求

1. 每个步骤应有明确的“下一步”动作。
2. 不允许用户在未完成当前步骤时直接结束该词学习。
3. 步骤完成状态需要写入学习会话数据。

### MVP 实现建议

| 步骤 | MVP 实现方式 |
|---|---|
| 看 | 图 + 单词 + 音标 |
| 听 | 真人发音或标准音频播放 |
| 说 | 浏览器语音能力或简化录音反馈 |
| 写 | 拼写输入框 + 字母校验 |
| 用 | 四选一语境题 |

## 9.3 SRS 复习引擎

### 目标

根据学生表现为每个单词安排下一次复习时间。

### 算法原则

1. 基于 SM-2 做小学生场景改良。
2. 评分简化为 3 档：
   - 记住了
   - 有点模糊
   - 忘了
3. 间隔上限需要控制，避免过早放飞。

### 关键字段

- `repetition`
- `efactor`
- `interval`
- `lastReviewAt`
- `nextReviewAt`
- `masteryLevel`

### MVP 规则

| 结果 | 处理 |
|---|---|
| 记住了 | 增加间隔 |
| 模糊 | 小幅增加间隔，保守处理 |
| 忘了 | 回到短间隔复习 |

## 9.4 每日任务系统

### 目标

控制每日学习量，形成“学完即走”的封闭体验。

### 关键能力

1. 每日新词上限
2. 每日复习上限
3. 每日时长上限
4. 可学习时段
5. 学完锁定

## 9.5 完成页

### 目标

在每日任务完成后，给出总结并结束本次学习，而不是继续追加无限任务。

### 必含信息

1. 今日新学单词数
2. 今日复习单词数
3. 今日正确率
4. 成长反馈
5. 明确退出按钮

### 禁止事项

1. 不出现“再学 5 个”
2. 不出现无限奖励循环
3. 不出现强挽留弹窗

## 9.6 家长基础查看页

### 目标

让家长低成本理解孩子学习状态，而不是干预每个学习动作。

### 必含信息

1. 今日/本周完成情况
2. 学习时长
3. 正确率
4. 薄弱词列表
5. 每日规则设置入口

---

## 10. 数据设计

### 10.1 核心实体

| 实体 | 说明 |
|---|---|
| `textbookEdition` | 教材版本 |
| `textbookUnit` | 教材单元 |
| `word` | 单词基础信息 |
| `wordDefinition` | 单词释义 |
| `wordResource` | 图片、音频、例句等资源 |
| `user` | 用户 |
| `userWordProgress` | 用户对单词的掌握进度 |
| `learningSession` | 一次学习会话 |
| `reviewTask` | 待复习任务 |
| `dailyPlan` | 每日任务配置 |

### 10.2 核心字段建议

#### `textbookEdition`

- `id`
- `name`
- `publisher`
- `startGrade`
- `isActive`

#### `textbookUnit`

- `id`
- `editionId`
- `grade`
- `semester`
- `unitNumber`
- `unitName`
- `sortOrder`

#### `word`

- `id`
- `word`
- `phoneticUs`
- `phoneticUk`
- `difficulty`
- `frequency`

#### `wordResource`

- `id`
- `wordId`
- `type` (`image`, `audio`, `sentence`)
- `url`
- `source`

#### `userWordProgress`

- `userId`
- `wordId`
- `repetition`
- `efactor`
- `interval`
- `nextReviewAt`
- `lastReviewAt`
- `masteryLevel`
- `seenImage`
- `heardAudio`
- `spokenAloud`
- `writtenSpell`
- `usedInContext`

#### `learningSession`

- `id`
- `userId`
- `unitId`
- `startedAt`
- `endedAt`
- `newWordsCount`
- `reviewWordsCount`
- `accuracy`

### 10.3 数据关系

1. 一个教材版本有多个单元。
2. 一个单元有多个单词。
3. 一个单词可关联多条资源。
4. 一个用户对多个单词有进度记录。
5. 一个用户每天可以有多个学习会话。

---

## 11. 技术架构

### 11.1 前端

- React 19
- TypeScript
- Vite
- Tailwind CSS
- Radix UI
- Zustand
- React Query

### 11.2 后端

- Node.js
- Hono
- PostgreSQL
- Redis

### 11.3 媒体与部署

- 媒体资源：腾讯云 COS
- 前端部署：Vercel
- 后端部署：Railway / Fly.io

### 11.4 PWA 预留

第一版不强制离线，但结构上预留：

1. `manifest.json`
2. Workbox service worker
3. 已学单词资源缓存策略

---

## 12. API 设计

### 12.1 教材与单元

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/textbooks` | 获取教材列表 |
| `GET` | `/api/textbooks/:id/units` | 获取某教材下单元列表 |
| `GET` | `/api/units/:id/words` | 获取某单元单词列表 |

### 12.2 学习与复习

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/learn/today` | 获取今日任务概览 |
| `POST` | `/api/learn/session/start` | 开始学习会话 |
| `POST` | `/api/learn/word/progress` | 提交单词学习步骤进度 |
| `POST` | `/api/review/grade` | 提交复习评分 |
| `GET` | `/api/review/tasks` | 获取待复习任务 |

### 12.3 家长与设置

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/parent/summary` | 获取家长摘要数据 |
| `GET` | `/api/parent/weak-words` | 获取薄弱词列表 |
| `GET` | `/api/settings/daily-plan` | 获取每日计划 |
| `POST` | `/api/settings/daily-plan` | 更新每日计划 |

### 12.4 认证

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/auth/login` | 登录 |
| `POST` | `/api/auth/logout` | 退出 |
| `GET` | `/api/auth/me` | 当前用户信息 |

第一版若不接真实登录，可先以本地 mock 用户或简化登录代替。

---

## 13. 状态与业务规则

### 13.1 单元状态

- `not_started`
- `in_progress`
- `completed`

### 13.2 单词掌握度

- `0` 新词
- `1` 初学
- `2` 熟悉
- `3` 掌握
- `4` 稳定掌握

### 13.3 每日任务规则

1. 复习优先于新词。
2. 达到每日上限后进入完成页。
3. 在家长设定的禁用时段内，不允许开始新会话。

### 13.4 错误与容错规则

1. 学习步骤提交失败时，前端应提示重试而非丢失全部会话。
2. 媒体资源加载失败时，允许回退到纯文本/纯音频模式。
3. 发音能力不可用时，允许跳过“说”步骤，但需记录降级状态。

---

## 14. UI/UX 要求

### 14.1 视觉原则

1. 主体界面信息密度低。
2. 单词主标题突出，辅助信息分层清楚。
3. 按钮操作方向单一，避免岔路太多。
4. 色彩积极，但不使用过于刺激的动效。

### 14.2 儿童适配要求

1. 关键按钮尺寸足够大。
2. 每页只有一个主操作。
3. 步骤切换反馈必须明确。
4. 错误提示要能被孩子读懂。

### 14.3 反成瘾要求

1. 禁止无限滚动型学习任务。
2. 禁止自动连播视频或奖励动画。
3. 禁止退出挽留弹窗。
4. 必须有明确完成页。

---

## 15. 性能与质量指标

| 指标 | 目标 |
|---|---|
| 首屏加载 | 小于等于 2 秒 |
| 页面切换 | 小于等于 300ms |
| 音频播放响应 | 小于等于 500ms |
| SRS 调度响应 | 小于等于 100ms |
| 关键流程稳定性 | 核心学习流程不可崩溃 |

---

## 16. 开发里程碑

### Phase 0：基础准备

1. 建立工程结构
2. 确定数据库 schema
3. 准备至少 1 套教材样例数据

### Phase 1：MVP 闭环

1. 教材入口
2. 五步学习流
3. SRS 调度
4. 完成页
5. 家长摘要页

### Phase 2：增强功能

1. 多教材
2. PWA 离线
3. 花园成长系统
4. 记忆曲线

### Phase 3：扩展

1. 词根词缀
2. 音素级发音评测
3. 教师端/班级视图
4. 原生 APP

---

## 17. 验收标准

### 17.1 功能验收

1. 能选择教材和单元。
2. 能完成至少一个单词的五步学习流。
3. 能在学习后生成复习任务。
4. 能限制每日学习量。
5. 能展示完成页。
6. 能让家长查看基本学习记录。

### 17.2 文档验收

1. 本 `design.md` 可直接拆解为 `step-X.md`。
2. 设计范围清晰，没有与非目标冲突。
3. 数据结构、接口和页面已足够支持工程实现。

### 17.3 演示验收

课程演示中需能完成如下主流程：

```text
首页 -> 选单元 -> 学习 1-2 个单词 -> 进入复习 -> 达到上限 -> 完成页 -> 查看家长页
```

---

## 18. 风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| 教材数据准备不足 | 会阻塞演示 | 先做最小样例单元 |
| 发音能力不稳定 | 浏览器权限受限 | 提供降级路径 |
| 功能范围膨胀 | 课程项目时间有限 | 严格执行 MVP 边界 |
| 小学生交互过复杂 | 完成率下降 | 保持单页单主操作 |
| 家长功能过重 | 开发成本增加 | 第一版只做查看与设置 |

---

## 19. 暂不实现内容

以下内容可以记录在产品计划中，但不进入第一版实施：

1. 原生 APP
2. 多孩子家庭完整管理体系
3. 教师创建词书
4. 班级排行榜
5. 视频内容大规模接入
6. 复杂会员订阅

---

## 20. 后续拆分建议

基于本设计文档，建议下一步直接拆成以下文件：

1. `step-1.md`：项目初始化
2. `step-2.md`：词库与教材结构
3. `step-3.md`：教材入口与今日任务页
4. `step-4.md`：五步学习流
5. `step-5.md`：SRS 复习引擎
6. `step-6.md`：完成页与每日任务上限
7. `step-7.md`：家长页
8. `step-8.md`：错误处理、UI 优化与 PWA 预留

这 8 个步骤足以支撑一个完整的课程项目开发周期。
